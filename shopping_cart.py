cart {}

1) Takes in an input

prompt = "Would you like to add/remove item or checkout"

While input != "checkout"

if message = "add":
    prompt = int "How much was item?" 
    # create dictionary for item with key as name, and price as value
    cart.append = message2
    #is this an instance where I would return to the cart?

    prompt = 'Would you like to add another item'

if remove:
    prompt = "Which item would you like to remove?"
    # search through cart dictionary and remove item

if checkout:   
    print( 
        total price, item, quantity 
     )

#make a function where returned cart is visible (print)


#Cart is a dictionary with multiple dictionaries in it. 





prompt +=

2) Stores user input into a dictionary or list
3) The User can add or delete items
4) The User can see current shopping list
5) The program Loops until user 'quits'
6) Upon quiting the program, prints out a receipt of the items with total and quantity. 